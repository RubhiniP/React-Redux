const React = require('react');

const App = () => {
    return(
        React.createElement('div', null, React.createElement('h1', null, 'Browserify worked'))
        
    );
}

module.exports = App;